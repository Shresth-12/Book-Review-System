<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book List</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:ital,wght@0,100..900;1,100..900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: Georgia, serif;
            overflow-x: hidden;
        }
        * {
            margin: 0;
            padding: 0;
        }
        .head h1 {
            font-size: 50px;
            position: relative;
            left: 350px;
            font-family: "Noto Sans";
            font-weight: 600;
        }
        .head h2 {
            font-size: 40px;
            position: relative;
            left: 350px;
            font-family: "Noto Sans";
            font-weight: 400;
        }
        .head p {
            font-size: 20px;
            position: relative;
            left: 350px;
            font-family: "Noto Sans";
            font-weight: 400;
            font-family: Georgia;
        }
        .head a {
            color: #3d00cc;
            text-decoration: none;
            font-weight: 700;
        }
        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            position: relative;
            left: 30px;
        }
        .header {
            color: #000000;
            text-align: center;
            margin-bottom: 20px;
        }
        .header h1 {
            font-size: 2em;
            margin: 0;
            text-transform: uppercase;
        }
        .header h2 {
            font-size: 1.2em;
            font-weight: normal;
            margin: 0;
            margin-top: 5px;
            color: red;
        }
        .content {
            display: flex;
            align-items: flex-start;
            word-wrap: break-word;
        }
        .content img {
            max-width: 200px;
            height: auto;
            margin-right: 20px;
            border-radius: 5px;
        }
        .text {
            flex: 1;
            word-wrap: break-word;
            max-width: 650px;
        }
        .text h3 {
            font-size: 1.1em;
            font-weight: normal;
            color: #7d7d7d;
            margin: 0 0 20px 0;
            word-wrap: break-word;
        }
        .text p {
            margin: 0 0 20px 0;
            overflow: hidden;
            text-overflow: ellipsis;
        }
        .button-container {
            display: flex;
            gap: 10px; /* Adjust the spacing between buttons */
        }
        .button {
            background-color: #3d00cc;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }
        .button:hover {
            background-color: #1f0073;
        }
        .add {
            position: relative;
            left: 1300px;
            top: -30px;
            background-color: rgb(214, 214, 247);
        }
    </style>
</head>
<body>
    <div class="head">
        <h1><%= ui %></h1>
        <a href="/add" class="button add">ADD</a>
        <br>
        <h2>Books I’ve read</h2>
        <br><br>
        <p>Tiny summary but <b>detailed notes for each</b>. Use the ISBN number to find it from your <br><br> local library or anywhere else. <b>This page will constantly update as I read more</b>, so <br><br> bookmark it if you want to check back in a few months. <br><br> If you have any questions. You can <a href="">email me</a>.</p>
        <br><br>
        <hr style="width:54%;text-align:left;margin-left:350px">
    </div>
    <% if (book.length > 0) { %>
        <% for (let index = 0; index < book.length; index++) { %>
            <% if (book[index].user_id === id) { %>
            <div class="container">
                <div class="header">
                    <h1><%= book[index].title %> - by <%= book[index].author %></h1>
                    <h2> How strongly I recommend it: <%= book[index].rating %>/10</h2>
                </div>
                <div class="content">
                    <img src="<%= book[index].image %>" alt="Book cover">
                    <div class="text">
                        <p><%= book[index].description %></p>
                        <div class="button-container">
                            <form action="/notes" method="post">
                                <button type="submit" class="button" value="<%= book[index].id %>" name="read">Read my notes</button>
                            </form>
                            <a href="#" class="button">Go to Amazon page</a>
                        </div>
                    </div>
                </div>
            </div>
        <% } %>
        <% } %>
    <% } else { %>
        <p>No books found.</p>
    <% } %>
</body>
</html>
